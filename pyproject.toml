[project]
name = "ice-ai"
version = "0.1.0"
description = "Decision-making and reasoning layer for ICE"
requires-python = ">=3.10"
dependencies = [
    "ice-api>=0.1.0",
]

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]
